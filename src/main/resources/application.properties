spring.application.name=backend
# Conexión a la base de datos
spring.datasource.url=jdbc:postgresql://localhost:5432/comutel_db
spring.datasource.username=postgres
spring.datasource.password=Comutel.2026
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración de JPA (Hibernate)
# Esto crea las tablas automáticamente si no existen
spring.jpa.hibernate.ddl-auto=update
# Esto nos deja ver las consultas SQL en la consola (útil para aprender)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
# --- CONFIGURACIÓN PARA ENVIAR CORREOS (SMTP) ---
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=helpdeskcomutel@gmail.com
spring.mail.password=erno mocr nlhv hduc
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true